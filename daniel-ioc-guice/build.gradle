dependencies {
    compile project(':daniel-core')
    // for injecting the ObjectMappers to main factory object
    compile group: 'com.google.inject.extensions', name: 'guice-multibindings', version: '4.0'
}
